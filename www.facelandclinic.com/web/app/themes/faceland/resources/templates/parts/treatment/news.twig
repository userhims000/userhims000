{% if not is_mobile %}
    {% if news.show_desktop %}
        <div id="{{ section_label }}" class="o-contain o-contain-padding news-section-wrap treatment_news specialist-section" bg-image-url="">
            <div class="c-intro--column  c-d-global-section">
                    <div class="u-text-center">
                    {% if news.title %}
                            {% set titleTag = news.news_tag ? news.news_tag : 'h2' %}
                            <{{ titleTag }} class="u-text-bold section-sub-title">{{ news.title }}</{{ titleTag }}>
                    {% endif %}
                    {% if news.intro %}
                            <div class="news_brief">{{ news.intro }}</div>
                    {% endif %}
                    </div>
            </div>
            <div class="news-listing-wrap">
                    <ul id="news_wrap_0" class="news_desktop_wrap">
                    {% set counter = 0 %}
                    {% for list in news.data %}
                        {% set attachmentId = list.blog_article_image %}
                        {% set attachmentUrl = function('wp_get_attachment_url', attachmentId) %}
                        {% if counter == 4 %}
                            </ul>
                            <ul id={{ 'news_wrap_' ~ counter }} class="news_desktop_wrap">
                            {% set counter = 0 %}
                        {% endif %}
                        <a class="news-link" href="{{ postPermalink }}"><div class="news-section">
                        {% set postPermalink = function('rokit_get_post_permalink', list.ID) %}
                            <div class="news-left">
                                {% if attachmentUrl %}
                                    <img class="" data-src-retina="" title="{{ list.post_title }}" alt="{{ list.post_title }}" src="{{ attachmentUrl }}">
                                {% endif %}
                            </div>
                            <div class="news-right">
                                {% set postTitle = list.post_title|slice(0, 22) ~ '..' %}
                                {% set postTitleLower  = postTitle | capitalize %}
                                {% set postDescription = function('rokit_get_news_description', list.ID)|slice(0, 26) ~ '..' %}
                                <h5 class="news-title">{{ postTitleLower }}</h5>
                                <p class="news-description">{{ postDescription }}</p>
                            </div>
                        </div></a>
                        {% set counter = counter + 1 %}
                    {% endfor %}
                    </ul>
            </div>
        </div>
    {% endif %}
{% endif %}
{% if is_mobile %}
    {% if news.show_mobile %}
        <div id="{{ section_label }}" class="o-contain o-contain-padding treatment_news news-section-wrap" bg-image-url="">
            <div class="c-intro--column  c-d-global-section">
                    <div class="u-text-center">
                    {% if news.title %}
                            <{{ news.news_tag }} class="u-text-bold section-sub-title">{{ news.title }}</{{ news.news_tag }}>
                    {% endif %}
                    {% if news.intro %}
                        <p>{{ news.intro }}</p>
                    {% endif %}
                    </div>
            </div>
            <div class="news-listing-wrap">
                {% for list in news.data %}
                {% set attachmentId = list.blog_article_image %}
                {% set attachmentUrl = function('wp_get_attachment_url', attachmentId) %}
                {% set postPermalink = function('rokit_get_post_permalink', list.ID) %}
                    <a class="news-link" href="{{ postPermalink }}"><div class="news-section">
                        <div class="news-left">
                            {% if attachmentUrl %}
                                <img class="" data-src-retina="" title="{{ list.post_title }}" alt="{{ list.post_title }}" src="{{ attachmentUrl }}">
                            {% endif %}
                        </div>
                        <div class="news-right">
                            {% set postTitle = list.post_title|slice(0, 33) ~ '...' %}
                            {% set postTitleLower = postTitle | capitalize %}
                            {% set postDescription = function('rokit_get_news_description', list.ID)|slice(0, 40) ~ '...' %}
                            <h5 class="news-title">{{ postTitleLower }}</h5>
                            <p class="news-description">{{ postDescription }}</p>
                        </div>
                    </div></a>
                {% endfor %}
            </div>
        </div>
    {% endif %}
{% endif %}