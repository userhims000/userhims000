<article class="o-card">

    <div class="o-card__content u-bg-white">
        <h3>{{ card.card_title }}</h3>
        <h4>{{ card.card_subtitle }}</h4>
        <p>{{ card.card_intro }}</p>
        <a href="{{ card.link }}" class="c-btn c-btn--small" title="{{ card.card_title }}">{{ __('View post', 'faceland') }}</a>
    </div>

</article>
