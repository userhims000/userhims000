{% set title = big ? get_connected.big : get_connected.small %}

{{ title }}

{# <pre>{{ dump(contacts.online.phone) }}</pre>
<pre>{{ dump(contacts.online.whatsapp) }}</pre>
<pre>{{ dump(contacts.online.chat) }}</pre> #}
