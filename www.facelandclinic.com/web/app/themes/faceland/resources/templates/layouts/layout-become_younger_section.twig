{% if layout|length > 0 and layout.become_younger_image  %}
    <div class="c-intro--column become-younger-section">
        <div class="quote-image-section">
            <figure class="o-panorama__image">
                <img class="js-lazy-load is-lazy-loaded" data-src="{{ layout.become_younger_image }}" data-src-retina=""  src="{{ layout.become_younger_image }}">
            </figure>
        </div>    
    </div>
{% endif %}
